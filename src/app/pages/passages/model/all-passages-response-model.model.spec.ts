import { AllPassagesResponseModel } from './all-passages-response-model.model';

describe('AllPassagesResponseModel', () => {
  it('should create an instance', () => {
    expect(new AllPassagesResponseModel()).toBeTruthy();
  });
});
