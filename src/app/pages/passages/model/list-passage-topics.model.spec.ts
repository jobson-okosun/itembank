import { ListPassageTopics } from './list-passage-topics.model';

describe('ListPassageTopics', () => {
  it('should create an instance', () => {
    expect(new ListPassageTopics()).toBeTruthy();
  });
});
