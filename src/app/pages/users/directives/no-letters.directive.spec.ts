import { NoLettersDirective } from './no-letters.directive';

describe('NoLettersDirective', () => {
  it('should create an instance', () => {
    const directive = new NoLettersDirective();
    expect(directive).toBeTruthy();
  });
});
