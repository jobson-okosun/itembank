import { AddSubjectToUser } from './add-subject-to-user.model';

describe('AddSubjectToUser', () => {
  it('should create an instance', () => {
    expect(new AddSubjectToUser()).toBeTruthy();
  });
});
