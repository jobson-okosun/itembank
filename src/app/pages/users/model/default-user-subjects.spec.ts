import { DefaultUserSubjects } from './default-user-subjects';

describe('DefaultUserSubjects', () => {
  it('should create an instance', () => {
    expect(new DefaultUserSubjects()).toBeTruthy();
  });
});
