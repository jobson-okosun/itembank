import { UserActivityFilter } from './user-activity-filter';

describe('UserActivityFilter', () => {
  it('should create an instance', () => {
    expect(new UserActivityFilter()).toBeTruthy();
  });
});
