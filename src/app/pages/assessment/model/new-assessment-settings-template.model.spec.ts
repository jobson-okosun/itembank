import { NewAssessmentSettingsTemplate } from './new-assessment-settings-template.model';

describe('NewAssessmentSettingsTemplate', () => {
  it('should create an instance', () => {
    expect(new NewAssessmentSettingsTemplate()).toBeTruthy();
  });
});
