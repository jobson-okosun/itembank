import { AssessmentPassageFilterInformation } from './assessment-passage-filter-information';

describe('AssessmentPassageFilterInformation', () => {
  it('should create an instance', () => {
    expect(new AssessmentPassageFilterInformation()).toBeTruthy();
  });
});
