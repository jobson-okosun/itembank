import { AssessmentFilterResult } from './assessment-filter-result';

describe('AssessmentFilterResult', () => {
  it('should create an instance', () => {
    expect(new AssessmentFilterResult()).toBeTruthy();
  });
});
