import { SingleAssessmentDetailTemplate } from './single-assessment-detail-template.model';

describe('SingleAssessmentDetailTemplate', () => {
  it('should create an instance', () => {
    expect(new SingleAssessmentDetailTemplate()).toBeTruthy();
  });
});
