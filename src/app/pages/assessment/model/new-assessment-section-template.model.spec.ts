import { NewAssessmentSectionTemplate } from './new-assessment-section-template.model';

describe('NewAssessmentSectionTemplate', () => {
  it('should create an instance', () => {
    expect(new NewAssessmentSectionTemplate()).toBeTruthy();
  });
});
