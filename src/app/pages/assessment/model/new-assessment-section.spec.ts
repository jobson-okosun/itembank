import { NewAssessmentSection } from './new-assessment-section';

describe('NewAssessmentSection', () => {
  it('should create an instance', () => {
    expect(new NewAssessmentSection()).toBeTruthy();
  });
});
