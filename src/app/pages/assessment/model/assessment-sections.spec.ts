import { AssessmentSections } from './assessment-sections';

describe('AssessmentSections', () => {
  it('should create an instance', () => {
    expect(new AssessmentSections()).toBeTruthy();
  });
});
