import { AssessmentSettings } from './assessment-settings';

describe('AssessmentSettings', () => {
  it('should create an instance', () => {
    expect(new AssessmentSettings()).toBeTruthy();
  });
});
