import { NewAssessmentTemplate } from './new-assessment-template';

describe('NewAssessmentTemplate', () => {
  it('should create an instance', () => {
    expect(new NewAssessmentTemplate()).toBeTruthy();
  });
});
