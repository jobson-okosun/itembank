import { AssessmentTemplatePage } from './assessment-template-page.model';

describe('ListAssessmentTemplate', () => {
  it('should create an instance', () => {
    expect(new AssessmentTemplatePage()).toBeTruthy();
  });
});
