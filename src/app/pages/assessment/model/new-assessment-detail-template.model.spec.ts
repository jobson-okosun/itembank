import { NewAssessmentDetailTemplate } from './new-assessment-detail-template.model';

describe('NewAssessmentDetailTemplate', () => {
  it('should create an instance', () => {
    expect(new NewAssessmentDetailTemplate()).toBeTruthy();
  });
});
