import { AssessmentTemplate } from './assessment-template.model';

describe('AssessmentTemplate', () => {
  it('should create an instance', () => {
    expect(new AssessmentTemplate()).toBeTruthy();
  });
});
