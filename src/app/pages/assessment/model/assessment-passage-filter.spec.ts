import { AssessmentPassageFilter } from './assessment-passage-filter';

describe('AssessmentPassageFilter', () => {
  it('should create an instance', () => {
    expect(new AssessmentPassageFilter()).toBeTruthy();
  });
});
