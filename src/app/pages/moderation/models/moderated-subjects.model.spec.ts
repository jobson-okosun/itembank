import { ModeratedSubjects } from './moderated-subjects.model';

describe('ModeratedSubjects', () => {
  it('should create an instance', () => {
    expect(new ModeratedSubjects()).toBeTruthy();
  });
});
