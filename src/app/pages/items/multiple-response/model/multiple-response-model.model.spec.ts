import { MultipleResponseModel } from './multiple-response-model.model';

describe('MultipleResponseModel', () => {
  it('should create an instance', () => {
    expect(new MultipleResponseModel()).toBeTruthy();
  });
});
