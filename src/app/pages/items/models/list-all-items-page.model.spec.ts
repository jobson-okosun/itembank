import { ListAllItemsPage } from './list-all-items-page.model';

describe('ListAllItemsPage', () => {
  it('should create an instance', () => {
    expect(new ListAllItemsPage()).toBeTruthy();
  });
});
