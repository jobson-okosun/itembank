import { AuthorModerationNotification } from './author-moderation-notification';

describe('AuthorModerationNotification', () => {
  it('should create an instance', () => {
    expect(new AuthorModerationNotification()).toBeTruthy();
  });
});
