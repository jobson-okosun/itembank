import { DefaultItemProperties } from './default-item-properties';

describe('DefaultItemProperties', () => {
  it('should create an instance', () => {
    expect(new DefaultItemProperties()).toBeTruthy();
  });
});
