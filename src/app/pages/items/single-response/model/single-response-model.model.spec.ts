import { SingleResponseModel } from './single-response-model.model';

describe('SingleResponseModel', () => {
  it('should create an instance', () => {
    expect(new SingleResponseModel()).toBeTruthy();
  });
});
