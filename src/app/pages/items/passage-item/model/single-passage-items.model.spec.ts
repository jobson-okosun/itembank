import { SinglePassageItems } from './single-passage-items.model';

describe('SinglePassageItems', () => {
  it('should create an instance', () => {
    expect(new SinglePassageItems()).toBeTruthy();
  });
});
