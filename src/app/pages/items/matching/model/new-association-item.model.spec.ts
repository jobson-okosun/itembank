import { NewAssociationItem } from './new-association-item.model';

describe('NewAssociationItem', () => {
  it('should create an instance', () => {
    expect(new NewAssociationItem()).toBeTruthy();
  });
});
