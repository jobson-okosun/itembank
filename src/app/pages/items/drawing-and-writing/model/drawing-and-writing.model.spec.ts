import { DrawAndWritingModel } from "./drawing-and-writing..model";

describe('RichEssayModel', () => {
  it('should create an instance', () => {
    expect(new DrawAndWritingModel()).toBeTruthy();
  });
});
