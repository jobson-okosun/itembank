import { NewRecyclePolicy } from './new-recycle-policy.model';

describe('NewRecyclePolicy', () => {
  it('should create an instance', () => {
    expect(new NewRecyclePolicy()).toBeTruthy();
  });
});
