import { ListRecycleSubjects } from './list-recycle-subjects.model';

describe('ListRecycleSubjects', () => {
  it('should create an instance', () => {
    expect(new ListRecycleSubjects()).toBeTruthy();
  });
});
